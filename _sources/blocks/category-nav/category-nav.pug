mixin category-nav(params)
    +b.category-nav
        +e.UL.list
            each item in params.items
                +e.LI.item
                    +e.A.link(href='#')= item.name
                    if item.count
                        +e.SPAN.count= item.count